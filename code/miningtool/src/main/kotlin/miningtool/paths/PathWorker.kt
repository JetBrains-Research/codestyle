package miningtool.paths

import miningtool.common.ASTPath
import miningtool.common.Node

class PathWorker {
    fun retrievePaths(tree: Node, pathRetrievalSettings: PathRetrievalSettings): Collection<ASTPath> {
        return emptyList()
    }
}